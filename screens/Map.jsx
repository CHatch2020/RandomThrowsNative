import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const Map = ({}) => {
  return (
    <SafeAreaView>
        <Text>Render Map to find local courses</Text>
    </SafeAreaView>
  );
};

export default Map;