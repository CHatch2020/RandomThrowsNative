import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const Profile = ({}) => {
  return (
    <SafeAreaView>
        <Text>Render User profile and data</Text>
    </SafeAreaView>
  );
};

export default Profile;